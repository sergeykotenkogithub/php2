В папке controller в actionDelete закоментировано как можно GET принимать.
А в delete.js закоментировано как GET отправлять.
Ещё указан 2 вид записи, но для удобства лучше тем что включен первый.

..................................................................

Пример через GET:

$id = $_GET['id'];

$count = Basket::countSum('quantity', 'session_id', $session);
$sum = Basket::countSum('price', 'session_id', $session);
echo json_encode(['count' => $count, 'sum' => $sum], JSON_UNESCAPED_UNICODE | JSON_PRETTY_PRINT);